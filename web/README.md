# web

SvelteKit dashboard for MacroSignal. See [AGENTS.md](AGENTS.md) for architecture, contracts, and verification.

```bash
bun run dev          # dev server (or devenv up from repo root)
bun run qa           # oxfmt + oxlint + svelte-check
bun run build        # production build (svelte-adapter-bun)
```
